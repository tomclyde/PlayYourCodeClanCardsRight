import React from "react";

const GameUI = (props) => {

  return (
    <div>
    <h2>Controls here</h2>
      <button type="button" onclick={(props.handinfo)}>
        Higher
      </button>
    </div>
  );

};

export default GameUI;
